import banner from '../../assets/banner.png'

const Bannner = () => {
    return (
        <div style={{backgroundImage:`url(${banner})`}} className='h-[500px] hero'>
            
        </div>
    );
};

export default Bannner;